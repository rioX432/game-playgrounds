{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Enables base IPC for the measurement window so the init-script poller can invoke the app's report/log commands.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
